import { add } from "./wasm";

function decryptMetadata(metadata: string): string {
  return metadata;
}

function hashPassword(password: string): string {
  console.log(add(1, 2));
  return password;
}
